version: 2.1
orbs:
  node: circleci/node@4.1.0
jobs:
  test:
    executor:
      name: node/default
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - node/test:
          version: 12.20.0
          pkg-manager: yarn
workflows:
  standard:
    jobs:
      - test
